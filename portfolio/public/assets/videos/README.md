# Project videos

Place web-optimized project videos in this folder, preferably as MP4 or WebM files.

Add the public path to the matching entry in `src/data/projects.json`:

```json
"video": "/assets/videos/revisit.mp4"
```

Projects without a `video` property keep their current card behavior and do not show a preview.
